import { H1 } from "~ui/components/Typography";

export const UnauthorizedInnerPage = () => (
  <div className="flex h-full flex-col items-center justify-center">
    <H1>Your account is not currently authorized</H1>
    <h2 className="mt-4 text-3xl">Noot is currently in closed alpha.</h2>
  </div>
);
